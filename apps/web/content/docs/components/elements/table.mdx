---
title: "Table"
description: "Data table with headers (Elements base)."
---

<ComponentPreview base="elements" name="table" hideCode />

## Installation

```bash
npx shadcn@latest add @pdfcn/elements/table
```

Maps to Unlayer `<Table>` (`headers` + `data`). Headers repeat on each
printed page by default. See https://github.com/unlayer/elements.
